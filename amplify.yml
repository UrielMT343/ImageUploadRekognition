version: 1
frontend:
  phases:
    prebuild:
      - cd image-upload-rekognition
      - npm install
    build:
      commands:
        - "npm run build"
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
